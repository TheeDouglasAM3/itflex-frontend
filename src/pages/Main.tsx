import React, { ReactElement } from 'react'

const Main = (): ReactElement => {
  const title = 'Página principal'
  return (
    <div id="page-main">
      {title}
    </div>
  )
}

export default Main
